name: E2E Tests

on:
  pull_request:
    branches: [develop, main]
  workflow_dispatch:

concurrency:
  group: e2e-tests
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - uses: gradle/actions/setup-gradle@v4

      - name: Run e2e tests
        env:
          TEST_BOT_TOKEN: ${{ secrets.TEST_BOT_TOKEN }}
          TEST_HELPER_BOT_TOKEN: ${{ secrets.TEST_HELPER_BOT_TOKEN }}
          TEST_CHAT_ID: ${{ secrets.TEST_CHAT_ID }}
          TEST_ADMINS: ${{ secrets.TEST_ADMINS }}
          E2E_DATABASE_URL: ${{ secrets.E2E_DATABASE_URL }}
          E2E_DATABASE_USERNAME: ${{ secrets.E2E_DATABASE_USERNAME }}
          E2E_DATABASE_PASSWORD: ${{ secrets.E2E_DATABASE_PASSWORD }}
        run: |
          chmod +x gradlew
          ./gradlew e2eTest

      - name: Upload test reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-test-reports
          path: build/reports/tests/e2eTest/
